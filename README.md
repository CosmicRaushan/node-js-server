# Node.js Server

A small Express server with basic health checks.

## Available Scripts

- `npm start` - run the server

## Endpoints

- `GET /` - returns a welcome message
- `GET /health` - returns service health information
